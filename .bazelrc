# Use Python 3.12
build --python_version=3.12

# Enable sandboxing for hermetic builds
build --sandbox_debug

# Use a local cache
build --disk_cache=/tmp/bazel-cache

# Test output
test --test_output=all