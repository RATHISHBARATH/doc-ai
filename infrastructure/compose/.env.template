# ============================================================
# DOC AI Local Development Environment Variables (Phase 1)
# ============================================================

# PostgreSQL
POSTGRES_PASSWORD=changeme

# MinIO (S3-compatible object storage)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=changeme

# Grafana
GRAFANA_ADMIN_PASSWORD=admin

# JWT Secret (used by gateway and auth)
JWT_SECRET=changeme

# OAuth2 Client Credentials (for Google and GitHub)
# Obtain these from your Google Cloud Console and GitHub OAuth Apps
GOOGLE_CLIENT_ID=changeme
GOOGLE_CLIENT_SECRET=changeme
GITHUB_CLIENT_ID=changeme
GITHUB_CLIENT_SECRET=changeme

# (Optional) Override inference model or GPU usage
# MODEL_NAME=distilgpt2
# USE_GPU=auto